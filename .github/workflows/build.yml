name: 🔧 Build & Lint Sass

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Sass Compile Check
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v3

      - name: 🔧 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm install

      - name: 🧪 Run Sass build
        run: npm run build
